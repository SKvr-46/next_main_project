import React from 'react'
import styles from "styles/lorder.module.scss"

const Loader: React.FC = () => {
    return (
        <div className="loader">
        <div className="spinner"></div>
        </div>
    );
    };

export default Loader;
